// Declares user and thoughts to be requirements for the application. 
// Exports both User and Thought to be used throughout the application. 
const User = require('./User');
const Thought = require('./Thought');

module.exports = { User, Thought };
